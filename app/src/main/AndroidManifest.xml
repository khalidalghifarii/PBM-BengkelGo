<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BengkelGo"
        tools:targetApi="31">
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ResetPasswordActivity" />
        <activity android:name=".CongratulationsActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ProfileActivity" />

        <activity android:name=".VehicleRepairFormActivity" android:theme="@style/Theme.BengkelGo" />
        <activity android:name=".OrderSummaryActivity" android:theme="@style/Theme.BengkelGo" />
        <activity android:name=".OrderConfirmationActivity" android:theme="@style/Theme.BengkelGo" />

        <activity android:name=".SparePartListActivity" />
        <activity android:name=".SparePartDetailActivity" />
        <activity android:name=".ShoppingCartActivity" />
        <activity android:name=".SparePartCheckoutActivity" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>
    </queries>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>